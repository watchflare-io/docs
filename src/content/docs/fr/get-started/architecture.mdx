---
title: "Architecture et sécurité"
description: "Comment les composants de Watchflare s'articulent entre eux : les agents transmettent leurs données via gRPC/TLS 1.3 au Hub, qui stocke les métriques dans TimescaleDB et diffuse des mises à jour en temps réel via SSE."
---

import Callout from "@components/Callout.astro";

Watchflare tient en quatre blocs : les agents, le Hub, une base de séries temporelles, et un tableau de bord accessible via un navigateur, alimenté par Server-Sent Events (SSE).

```
┌──────────────────────────────────────────────────────┐
│                   Monitored Hosts                    │
│   [ Agent ]      [ Agent ]      [ Agent ]            │
└──────────┬───────────┬──────────────┬───────────────┘
           │           │              │
           │    gRPC / TLS 1.3        │
           ▼           ▼              ▼
┌──────────────────────────────────────────────────────┐
│                       Hub (Go)                       │
│                                                      │
│   gRPC server   ──▶   HeartbeatCache                 │
│   HTTP server   ──▶   TimescaleDB                    │
│   SSE broker    ──▶   Browser                        │
└──────────────────────────────────────────────────────┘
```

---

## Composants

### Hub

Le Hub est un seul binaire Go : l'interface est embarquée, deux ports ouverts :

- **`:8080`** : API HTTP, tableau de bord web et flux SSE
- **`:50051`** : serveur gRPC — heartbeats, métriques et inventaire des paquets

Le Hub tourne avec une instance **TimescaleDB** (PostgreSQL avec extensions time-series) pour le stockage. Les deux se déploient en conteneurs Docker.

### Agent

L'agent est un démon Go léger, sur chaque hôte surveillé. Il n'ouvre aucun port : il n'établit que des connexions sortantes. Sous Linux, utilisateur système `watchflare`. Sous macOS, Homebrew, compte courant.

L'agent fait tourner ces boucles indépendamment :

| Boucle | Intervalle | Rôle |
|------|----------|--------------|
| Heartbeat | 5 s | Envoie un ping de présence avec les adresses IP actuelles |
| Métriques | 30 s | Collecte et envoie les métriques système |
| Inventaire des paquets | 60 s après le démarrage, puis quotidiennement à 03 h 00 | Analyse les paquets installés, envoie les modifications |
| Inventaire des services | 60 s après le démarrage, puis toutes les 15 min | Répertorie les unités systemd (Linux avec systemd uniquement) |
| État des services | 30 s | Signale l'état en temps réel des services systemd (Linux avec systemd uniquement) |

### Base de données

TimescaleDB stocke les métriques dans une « hypertable » automatiquement partitionnée par intervalle de temps. Les agrégats continus précalculent des tranches de 10 minutes, 15 minutes, 2 heures et 8 heures. Le Hub interroge ces agrégats plutôt que les lignes brutes pour les plages de temps plus longues.

### Tableau de bord

L'interface (SvelteKit) tient une connexion SSE persistante vers le Hub. Statut des hôtes, métriques, agrégats : tout arrive en événements. Pas d'interrogation périodique.

---

## Flux de données

### Heartbeat et détection en ligne / hors ligne

```
Agent (every 5s)  ──▶  Hub  ──▶  HeartbeatCache (memory)
                         │
                         └──▶  SSE → browser (status: online)

StaleChecker (every 10s):
  no heartbeat > 15s  ──▶  mark offline in cache
                      └──▶  SSE → browser (status: offline)

SyncWorker (every 5min):
  flush cache (status, last_seen, IPs) ──▶  database
```

À chaque heartbeat, le Hub met à jour le cache mémoire et envoie tout de suite un événement SSE au tableau de bord. Pas d'écriture en base à ce moment-là. Les identifiants sont vérifiés en une seule lecture. Toutes les 5 minutes, le SyncWorker recopie statut, horodatages et IP vers la base. Si le StaleChecker ne voit plus de heartbeat depuis plus de 15 s, il marque l'agent hors ligne dans le cache et diffuse l'événement hors ligne directement, sans passer par la base.

### Collecte des métriques

```
Agent:
  1. Collect metrics
  2. Append to WAL (local file)
  3. Send to Hub via gRPC
  4. Clear WAL only if send succeeds

Hub:
  → INSERT into TimescaleDB
  → SSE metrics_update → browser
```

Si le Hub est injoignable, les métriques s'accumulent dans le WAL de l'agent. À la prochaine connexion réussie, les points en attente sont rejoués dans l'ordre, puis les nouvelles métriques partent.

### Inventaire des paquets

```
Agent (60s after start, then daily at 03:00):
  First run  → full inventory   → Hub upserts all packages
  Next runs  → delta only       → Hub processes added/removed/updated
```

Après le premier inventaire complet, seules les modifications partent. Les envois quotidiens restent petits, quel que soit le nombre de paquets.

### Services systemd (Linux)

```
Agent (systemd hosts only):
  Inventory (60s after start, then every 15 min)  → full unit catalog → Hub refreshes service list
  Health (every 30s)                              → live unit state   → SSE → browser
```

L'agent lit systemd via D-Bus, en utilisateur sans privilèges — jamais via `systemctl`. La boucle de santé retire en moins de 30 secondes un service qui n'est plus suivi. Voir [services systemd](/fr/monitoring/services/).

### Enregistrement d'un agent

L'enregistrement n'a lieu qu'une fois : il établit la confiance entre l'agent et le Hub.

```
1. Admin creates a host in the dashboard
   → Hub generates a registration token (wf_reg_...) valid for 24 hours

2. Token is pasted into the install command on the target host
   → Agent calls RegisterHost gRPC (TLS without cert verification, as the agent has no CA
     cert yet, so it authenticates with the registration token)
   → Hub validates token, returns: agent_id, agent_key, CA certificate

3. Agent saves credentials + CA cert to disk
   → CA is pinned immediately, so the agent rejects any certificate not signed by this CA
   → All future gRPC calls use mutual auth (HMAC-SHA256 + pinned CA)
```

---

## Modèle de sécurité

- **TLS 1.3** sur tout le trafic agent ↔ Hub
- **HMAC-SHA256** signe chaque requête gRPC (`agent_id` + horodatage + payload). Hors d'une fenêtre de ±5 minutes, la requête est rejetée.
- **Épinglage de la CA.** À l'enrôlement, l'agent épingle le certificat CA du Hub. Tout certificat qui n'est pas signé par cette CA est refusé.
- **Agent sans privilèges.** Sous Linux : utilisateur `watchflare`, pas de shell, pas de répertoire home, écriture limitée à son répertoire de données. Sous macOS : Homebrew, compte courant.
- **Jetons à usage unique.** Stockés en empreintes SHA-256. La valeur en clair s'affiche une fois, elle n'est jamais enregistrée.

<Callout type="info" title="Remarque">
  Au premier démarrage, le Hub génère sa CA TLS et son certificat serveur. Pour fournir les vôtres, passez `TLS_MODE=custom`. Voir [certificats TLS](/fr/hub/tls/).
</Callout>

---

## Détection d'environnement

L'agent ajuste la collecte selon là où il tourne :

| Environnement | Non collecté |
|-------------|-------|
| Conteneur d'application (Docker, Podman) | Disque, E/S disque, réseau, swap, température |
| Conteneur système (LXC) | Rien — traité comme un hôte complet |
| Machine virtuelle | Capteurs de température (pas d'accès au matériel) |
| Hôte physique | Rien — collecte complète |
