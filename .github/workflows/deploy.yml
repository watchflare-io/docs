name: Trigger Cloudflare Pages deploy

on:
  repository_dispatch:
    types: [watchflare-release]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger deploy
        run: curl -X POST "${{ secrets.CLOUDFLARE_DEPLOY_HOOK }}"
