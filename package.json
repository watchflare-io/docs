{
  "name": "",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "dev:search": "pnpm build && cp -r dist/pagefind public/ && astro dev",
    "build": "astro build && npx -y pagefind --site dist",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/mdx": "^5.0.4",
    "astro": "^6.1.9"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}